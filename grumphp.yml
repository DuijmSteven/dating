parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
      phpcs:
        standard: PSR2
        ignore_patterns: [./tests/*, ./resources/*, ./database/*, ./bootstrap/*, ./public/*, ./app/Helpers/ccampbell/*, ./vendor/*, ./_ide_helper.php]
      phpunit: ~