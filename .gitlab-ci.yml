before_script:
  - apt-get update -qq
  - apt-get install -qq git
  # Setup SSH deploy keys
  - 'which ssh-agent || ( apt-get install -qq openssh-client )'
  - eval $(ssh-agent -s)
  - ssh-add <(echo "$SSH_PRIVATE_KEY")
  - mkdir -p ~/.ssh
  - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'

stages:
  - deploy_staging_altijdsex_nl
  - deploy_staging_liefdesdate_nl
  - deploy_production_altijdsex_nl

deploy_staging_altijdsex_nl:
  stage: deploy_staging_altijdsex_nl
  environment:
    name: staging
  script:
    - ssh opalampo@128.199.53.121 "cd /var/www/dating &&
      git fetch &&
      git checkout staging &&
      git pull origin staging &&
      composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader &&
      php artisan migrate --force &&
      php artisan config:cache &&
      php artisan route:cache &&
      php artisan view:cache &&
      php artisan optimize &&
      php artisan queue:restart &&
      exit"
  only:
    - staging

deploy_staging_liefdesdate_nl:
  stage: deploy_staging_liefdesdate_nl
  environment:
    name: staging
  script:
    - ssh opalampo@134.209.87.252 "cd /var/www/dating &&
      git fetch &&
      git checkout staging &&
      git pull origin staging &&
      composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader &&
      php artisan migrate --force &&
      php artisan config:cache &&
      php artisan route:cache &&
      php artisan view:cache &&
      php artisan optimize &&
      php artisan queue:restart &&
      exit"
  only:
    - staging

deploy_production_altijdsex_nl:
  stage: deploy_production_altijdsex_nl
  environment:
    name: production
  script:
    - ssh opalampo@178.62.249.225 "cd /var/www/dating &&
      git fetch &&
      git checkout master &&
      git pull origin master &&
      composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader &&
      php artisan migrate --force &&
      php artisan config:cache &&
      php artisan route:cache &&
      php artisan view:cache &&
      php artisan optimize &&
      php artisan queue:restart &&
      exit"
  only:
    - master